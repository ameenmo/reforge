# Claude Code — Tool-Specific Hints

## Workflow

- Use `/plan` mode before implementing complex features. Plan first, then execute.
- Use `/commit` after completing discrete units of work.
- Prefer editing existing files over creating new ones.
- Check `.agent/tasks/` for active tasks before starting new work.
- Update task files with your findings when handing off to another tool.

## Context

- This generated `CLAUDE.md` provides the project SOP and architecture context.
- Read `.agent/readme.md` for the full project overview and quick commands.

## Code Patterns

<!-- Add project-specific code patterns here. Examples:
### Adding a new module
1. Create the module file
2. Register it
3. Add tests

### Database changes
1. Edit schema
2. Run migrations
3. Update seed data
-->

## Task Management

- Check `.agent/tasks/` for active tasks before starting new work.
- When creating a new task, copy `.agent/tasks/_template.md` and fill in details.
- Update task files with progress and findings as you work.
